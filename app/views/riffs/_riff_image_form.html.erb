<%= form_for [@user, @riff], url: user_riff_path(@user, @riff), html: { method: :put } do |form| %>
  <%= form.label :avatar, 'Upload Riff Image', style: "color: white;" %>
  <%= form.file_field :avatar %>
  <%= form.submit "Update" %>
